import {BrowserRouter} from "react-router-dom";

function Contact(){
    return(
        <div><br/>
                        STEPS TO CREATE COMPONENTS <br/><br/>
            * 1. npm i react-router-dom (to intsall the react-router-dom )<br/>
            * 2. Create home,about,contact and header separately
        </div>
    )
}
export default Contact